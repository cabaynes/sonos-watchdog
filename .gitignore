# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# Secrets — never commit env files
.env
.env.*
!.env.example
*.pem
*.key

# Logs
*.log

# Session putdowns stay local — this repo is PUBLIC; committed putdowns would publish session notes
.putdowns/

# Grill Me session logs stay local — this repo is PUBLIC; committed sessions would publish planning notes
grill-me-sessions/
